cmake_minimum_required(VERSION 2.8)
project( targetServer )
find_package( OpenCV REQUIRED )
add_executable( targetServer targetServer.cpp imageTracker.cpp )
target_link_libraries( targetServer ${OpenCV_LIBS} )
